# 票小帮 v4.0 · 个人财务自动化工作台（增量方案 · 修订版）

> 定位：个人自用工具 · 纯前端本地处理 · 双击即用 · 可部署 GitHub Pages
> 设计总纲：**以"减少工作量"为唯一标准——能复用的绝不重写，价值低的坚决砍掉，每阶段有可验收产出。**
> 用户补充要求（已并入）：① 直接读取上传的表格 / Word / PDF / 图片；② AI 接口留槽位由用户自填；③ 稳定性优先。

---

## 一、复用盘点（已有能力，零开发）

| 已有能力 | 所在模块 | v4.0 直接用于 |
|---|---|---|
| 发票台账 14 列 + IndexedDB 持久化 | store.js / app.js | 看板数据源，无需重新导入 |
| Excel 解析/导出（本地化） | lib/xlsx.min.js | 表格导入、对账、底稿导出 |
| PDF 文本提取 + 渲染 OCR | app.js / lib/pdf.min.js | PDF 导入 |
| 图片压缩 + OCR | app.js / ai.js | 图片导入 |
| OFD 解析 | ofd.js | 数电票导入 |
| 银行流水对账引擎 | recon.js | 对账 80% 已完成 |
| 报表统计引擎（snapshot/Top5/环比） | report.js | 指标卡与洞察直接复用 |
| 通用大模型对话 llmChat() | ai.js | 文字报告、归类兜底（**扩展 Anthropic 协议即可**） |
| **JSZip（Word 解析所需）** | lib/jszip.min.js | 新增 docx 解析，**零新依赖** |

**结论：对账与发票识别已建成；Word 解析只差一个解包器（JSZip 现成）；本次全部是"补缺口 + 组装"，无重复造轮子。**

---

## 二、本次增量（5 项）

### P0 统一数据导入层（本次新要求，最先做）
**四类文件一键导入 → 统一标准化为台账记录**，看板/对账/报表全部复用同一数据：

| 文件类型 | 解析方式 | 稳定性策略 |
|---|---|---|
| Excel / CSV（.xlsx/.xls/.csv） | xlsx 库（已有） | 表头自动识别 → 列映射预览 → 确认入库 |
| Word（.docx） | JSZip 解包 + XML 解析提取段落与**表格** | 表格优先转二维数组；纯文本降级按行解析 |
| PDF | pdf.js 文本提取（已有） | 有文本层→发票/表格解析；无文本层→渲染 OCR |
| 图片 | canvas 压缩 + OCR（已有） | OCR 失败给明确提示，可手动补录 |

**列映射（通用化核心）**：任意表头自动识别 → 下拉映射到 日期/金额/税额/分类/供应商/摘要 6 个标准字段，可保存映射模板复用。**不只服务发票，费用报销/采购/付款表都能导。**

### P1 财务看板（新页签「看板」）
- 直接读标准化台账，**5 指标卡**（价税合计/不含税/税额/票数/平均单票，带环比+迷你趋势）
- **6 类图表**：月度趋势折线、费用分类横向条、供应商 Top5 横向条、发票类型占比、税负率发散条、明细表
- **三维筛选**：时间区间 / 费用分类 / 供应商，图表联动
- 双主题（浅/深）CSS 变量切换；新增 `lib/echarts.min.js` + `dashboard.js`

### P2 余额调节表（对账延伸）
- 现有对账结果上补：**未达账项清单** + **银行余额调节表**（银行对账单余额 ± 企业未达账项 = 企业账面余额）
- recon.js 增加 `buildBalanceSheet()`（约 80 行），导出 Excel

### P3 流水归类 + 底稿生成（税务审计辅助）
- **流水归类**：关键词字典（摘要/户名→分类）→ 未命中交 llmChat → 人工确认回写字典（越用越准）
- **底稿模板**：费用抽查底稿、进项税勾选底稿、余额调节底稿（自动填充 + LLM 写说明）

### P4 AI 接口槽位（本次新要求）
设置面板新增 **"外部 AI 服务"** 区，用户自填，默认关闭：

| 配置项 | 说明 |
|---|---|
| 接口类型 | OpenAI 兼容（DeepSeek/混元/通义…）/ Anthropic（Claude） |
| Base URL | 默认 https://api.deepseek.com 或 https://api.anthropic.com |
| API Key | 用户自己填，仅存本机 localStorage |
| 模型名 | 用户自选（deepseek-chat / claude-sonnet-4-5 等） |
| 用途开关 | 字段纠错 / 文字报告 / 流水归类（可分别开关） |

协议已支持两种：OpenAI `chat/completions` + Anthropic `messages`（含 `anthropic-dangerous-direct-browser-access` 头）。**触发调用前弹提示"数据将发送至你配置的 API 服务商"**，其余功能零上传。

### P5 报告增强
- 复用 llmChat 生成完整版月度结账分析报告（概况→变化→异常→建议）

---

## 三、架构原则：模块独立，数据联动（用户明确要求）

> **"其中的功能要独立，但是又可以联系使用"** —— 这是本版的核心架构约束。

```
┌────────────────── 共享数据层（IndexedDB 本地库）──────────────────┐
│   台账数据  │  银行流水/对账结果  │  归类字典  │  报表/看板配置    │
└──────────────────────────────────────────────────────────────────┘
       ▲ ▲ ▲             ▲ ▲ ▲            ▲ ▲ ▲          ▲ ▲ ▲
       ┃ ┃ ┃  读/写       ┃ ┃ ┃ 读/写      ┃ ┃ ┃ 读/写    ┃ ┃ ┃ 读/写
┌──────┴─┴─┴─────┐ ┌─────┴─┴─┴─────┐ ┌───┴─┴─┴────┐ ┌───┴─┴─┴─────┐ ┌───┴─┴─┴────┐
│ ① 发票台账     │ │ ② 自动对账     │ │ ③ 报表中心  │ │ ④ 财务看板   │ │ ⑤ 税务辅助   │
│ （录入/查重）   │ │ （流水勾对）   │ │ （周月报）  │ │ （图表洞察） │ │ （归类/底稿）│
└───────────────┘ └───────────────┘ └────────────┘ └─────────────┘ └────────────┘
```

**独立（每个模块可单独使用）**
- 每个功能一个独立页签，界面/操作互不干扰，可单独完成自己的任务
- **每个模块自带"导入数据"入口**：不依赖其它模块也能跑通——
  - 对账页：可直接导入"银行流水 + 账目 Excel"独立对账（不必先有台账）
  - 看板页：可直接导入任意表格出看板（不必先有台账/对账）
  - 报表页：可直接导入数据出周报/月报
- 引擎文件各自独立（recon.js / report.js / parser.js），可单独测试、单独演进

**联动（数据一处导入，处处可用）**
- 所有模块读写同一个共享数据层（IndexedDB）：任何模块导入/产出的数据自动入库，其它模块随时取最新值
- 台账更新 → 对账/报表/看板自动读到新数据（无需重复导入）
- 对账未匹配项 → 一键送入看板/报表做分析
- 归类字典全局共享：人工确认一次归类，所有模块复用，越用越准
- 模块间通过"取数来源"切换：默认读共享数据，也可临时换成独立导入的数据

**落地方式**：单页工作台（index.html 多页签，天然满足"联系使用"）+ 每页签独立数据入口 + store.js 泛化为多数据集存取（满足"独立"）。不拆多个 HTML 文件，避免重复框架代码，工作量最小。

---

## 四、稳定性设计（本次重点要求）

1. **解析降级链**：PDF 无文本层 → 自动 OCR；docx 无表格 → 按行解析；OCR 失败 → 明确提示 + 手动补录入口
2. **导入预览确认**：表格类导入先展示"表头映射 + 前 N 行数据"预览，用户确认后才写入台账，**杜绝脏数据入库**
3. **字段容错**：金额/日期多格式解析（千分位、货币符号、yyyy年mm月dd日 等），解析失败的字段标黄可编辑
4. **全链路 try/catch**：每类文件独立失败，不阻塞其它文件；错误提示具体到文件名与原因
5. **数据零丢失**：写入 IndexedDB 前本地校验必填项；导出与导入格式互通可备份

---

## 五、砍掉清单（减负决策）

| 原方案内容 | 处理 | 理由 |
|---|---|---|
| 官方集成 Claude | 不接 | 留通用槽位用户自填，省授权代码 |
| 独立 dashboard.html 页面 | 不做 | 集成页签复用样式/框架/数据层 |
| B 层自然语言问答机器人 | 不做 | 个人使用，文字报告已够 |
| 重新搭建数据层 | 不做 | 直接读现有台账 |
| 资产负债表/利润表 | 本期不做 | 台账数据不足以支撑 |

**工作量对比：从零建约 3500+ 行 → 增量约 1200 行。**

---

## 六、实施顺序与验收

| 阶段 | 内容 | 可验收产出 |
|---|---|---|
| P0 | 四格式统一导入 + 列映射 + API 槽位 + 共享数据层 | 上传任意表格/Word/PDF/图片 → 预览映射 → 入台账；各模块可独立取数 |
| P1 | 财务看板 | 台账数据 30 秒出全量看板，5 卡 6 图 3 维筛选 |
| P2 | 余额调节表 | 对账后可导出调节表 Excel |
| P3 | 归类 + 底稿 | 流水一键归类、底稿 Excel 生成 |
| P5 | 报告增强 | 一键生成月度结账分析报告 |

P0 + P1 完成后即可日常使用，其余按需补齐。

---

## 七、决策点（已收敛，默认即推荐）

1. **交付形态**：单页工作台多页签 + 每模块独立导入入口 + 共享数据层联动 ✅
2. **AI**：留通用槽位（OpenAI 兼容 + Anthropic），用户自填 Key，默认关闭 ✅
3. **数据**：四格式统一导入 + 列映射，台账直读，多数据集共享 ✅
4. **范围**：仅本次 5 项增量 ✅

按 P0 开工。
