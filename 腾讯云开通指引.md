# 腾讯云开通指引（OCR 扫描件识别）

票小帮的「扫描件 OCR 识别」和「大模型字段纠错」是**可选功能**，不开通也能正常使用全部台账/查重/合并打印/OFD 解析功能。本文档指导你如何开通，全程约 15 分钟。

> 费用预估：OCR 每月有 **1000 次免费额度**（实名认证后），大模型纠错按 token 计费（DeepSeek 每张发票约 1-2 分钱）。日常使用基本可以做到 0 元或几元钱一个月。

---

## 一、注册腾讯云账号

1. 打开 <https://cloud.tencent.com/register> 注册并登录
2. 完成**实名认证**（个人认证即可，免费额度需要实名）
   - 地址：<https://console.cloud.tencent.com/developer/auth>
3. 认证一般在 1 分钟内完成

## 二、开通 OCR 服务（通用印刷体识别）

1. 进入 OCR 控制台：<https://console.cloud.tencent.com/ocr>
2. 首次进入会提示开通服务，点击**立即开通**
3. 找到「通用印刷体识别（GeneralBasicOCR）」，确认已开通

> 票小帮使用的就是 **GeneralBasicOCR** 接口，支持印刷体发票扫描件识别。

## 三、创建 API 密钥（SecretId / SecretKey）

1. 进入密钥管理：<https://console.cloud.tencent.com/cam/capi>
2. 点击「新建密钥」
3. 会生成一对：
   - **SecretId**（如 `AKIDxxxxxxxxxxxxxxxx`）
   - **SecretKey**（如 `xxxxxxxxxxxxxxxxxxxxxxxx`）
4. 把这两个值复制下来，填到票小帮「设置」里

> ⚠️ 密钥请妥善保管，不要发给别人。票小帮的密钥只保存在你自己的浏览器 localStorage 里，不会上传到任何服务器。

## 四、在票小帮中配置

1. 打开票小帮 → 右上角「⚙ 设置」
2. 「扫描件 OCR 识别」区块：
   - 启用 OCR → 选「开启」
   - 地域 → 保持默认 `ap-guangzhou`（广州）即可
   - SecretId / SecretKey → 粘贴第三步复制的值
3. 点「保存设置」

之后上传**扫描件 PDF**（文字版 PDF 提取不到内容时）会自动触发 OCR 识别。

## 五、（可选）大模型字段纠错

开启后，每张发票识别完成后会用大模型校验一遍号码/金额/日期等字段，提升准确率。

### 方案 A：DeepSeek（推荐，便宜）

1. 注册：<https://platform.deepseek.com/>（手机号注册即可）
2. 登录后左侧「API Keys」→「创建 API Key」→ 复制（格式 `sk-xxxxxxxx`）
3. 票小帮设置里：
   - 启用大模型 → 开启
   - 接口地址 → `https://api.deepseek.com`
   - 模型名称 → `deepseek-chat`
   - API Key → 粘贴
4. 首次使用需充值（最低 10 元），发票解析每次消耗约 1-2k token，费用约 1-2 分钱

### 方案 B：腾讯混元（用同一个腾讯云账号）

1. 进入混元大模型控制台：<https://console.cloud.tencent.com/hunyun>
2. 开通服务，在「API 密钥管理」创建密钥（调用时选 API Key 认证）
3. 票小帮设置里：
   - 接口地址 → `https://api.hunyuan.cloud.tencent.com/v1`（OpenAI 兼容端点）
   - 模型名称 → `hunyuan-turbos-latest` 或 `hunyuan-lite`（lite 免费额度大）
   - API Key → 粘贴

## 六、费用说明

| 项目               | 计费方式                                        | 预估成本               |
| ----------------- | ------------------------------------------- | ------------------ |
| 通用印刷体识别 OCR       | 每月 1000 次免费，超出约 ¥0.6/次                        | 日常报销基本免费          |
| DeepSeek 大模型纠错     | 输入约 ¥2/百万 token，输出约 ¥8/百万 token                | 每张发票约 1-2 分钱       |
| 腾讯混元 hunyuan-lite | 有免费额度，超出按 token 计费                            | 个人使用基本免费          |

## 七、（可选）部署到腾讯云 CloudBase 更稳定

当前版本已托管在 GitHub Pages 上，可正常访问。若想进一步用腾讯云托管（国内访问更快、更稳），可按以下步骤：

1. 进入 CloudBase 控制台：<https://console.cloud.tencent.com/tcb>
2. 创建环境，选**免费版（体验环境）**即可（3000 资源点/月，静态网站流量完全够用）
3. 开通「静态网站托管」→ 上传 `index.html`、`style.css`、`app.js`、`ledger.js`、`store.js`、`ofd.js`、`ai.js` 和 `lib/` 文件夹
4. 访问环境提供的默认域名（`.tcloudbaseapp.com`），即可像 GitHub Pages 一样使用

> 免费版额度：3000 资源点/月，纯静态网站月流量在 1GB 内完全免费；如需绑定自定义域名或更高流量，可升级个人版（¥19.9/月）。

## 八、常见问题

**Q：设置了 OCR 但上传扫描件没有触发？**
A：只有文本提取结果中缺少发票号码时才会触发 OCR（避免浪费免费额度）。确认「设置」里已开启并保存，且密钥填写正确。

**Q：OCR 报错「AuthFailure.SecretIdNotFound」？**
A：SecretId 或 SecretKey 填错，或密钥被删除。到腾讯云密钥管理重新生成并更新。

**Q：大模型报错「401」？**
A：API Key 错误或服务商接口地址不匹配。检查是否为 OpenAI 兼容接口，地址末尾不要带 `/`。

**Q：密钥存浏览器安全吗？**
A：只存本机浏览器，比明文写在代码里安全；且工具本身是纯前端，密钥不会出现在任何网络请求里（只有识别时直接调用腾讯云/大模型 API）。如仍不放心，可只在需要识别时临时开启。
