# 票小帮 · 完整使用说明

> 版本：v4.1（commit b89e093 之后）
> 更新日期：2026-08-18

票小帮是一款**纯本地运行**的 AI 发票与财务管理工具：发票文件不出本机（除非你主动启用 AI/OCR），无账号、无服务器、免费使用。

---

## 一、三种访问方式（选一种即可）

### 方式 1：线上版（最方便，推荐日常使用）

**https://wy133656.github.io/piaoxiaobang/**

- 打开即用，任何电脑/手机浏览器均可
- 数据存在**当前浏览器**里（见第四节"数据保存"）
- ⚠️ 首次加载需联网；之后浏览器有缓存，弱网也能打开大部分功能

### 方式 2：本地双击（完全离线）

双击项目文件夹里的 `index.html` 即可。

- 断网也能用（PDF 解析等核心库已本地化到 `lib/` 目录）
- ⚠️ 不要移动/删除 `lib/` 文件夹和各 js 文件，页面靠它们运行

### 方式 3：本地服务器（最稳定，推荐大文件多时使用）

项目文件夹里打开终端执行：

```
node test-server.js
```

然后浏览器访问 **http://localhost:8765**

- 比"双击打开"兼容性更好（个别浏览器限制 file:// 页面的部分能力）
- 完全离线可用，处理大批量发票时最稳

---

## 二、五大功能模块

### 1️⃣ 台账管理（主页签）

**做什么**：批量上传发票（PDF/OFD/图片/Excel 台账/Word），自动识别生成 14 列标准台账。

**怎么用**：
1. 点「选择文件」批量上传发票 → 自动识别：发票号码、开票日期、销售方/购买方、金额、税额、类型、简称、商品摘要、费用分类、特殊情况说明、归档文件名
2. 表格中点「编辑」可修正任何字段，带 ⚡ 按钮的字段可一键重新自动生成
3. 顶部筛选栏可按月份/分类/关键词过滤，统计卡实时汇总
4. 导出：
   - 「导出台账」→ 14 列样式 Excel（蓝底表头、合计行），可直接交财务
   - 「打包下载」→ 按"归档文件名"自动重命名的发票原件 zip，直接归档
5. 扫描件（图片型 PDF）：需在设置中开启腾讯云 OCR，每月 1000 次免费

**支持格式**：`.pdf` `.ofd`（数电票）`.jpg/.png`（需 OCR）`.xlsx/.xls/.csv`（台账表格）`.docx`

### 2️⃣ 发票查重

上传发票后自动检测重复报销（同一发票号码多次出现），防止重复入账。

### 3️⃣ 合并打印

多张发票合并排版打印，省纸省事，适合月底装订凭证。

### 4️⃣ 自动对账

**做什么**：银行流水 ⇄ 业务账目自动匹配，把"人眼找不同"变成"系统标异常"。

**怎么用**：
1. 「银行流水」上传网银下载的 Excel 流水（需含：交易日期 / 对方户名 / 收入金额 / 支出金额，摘要列会自动提取）
2. （可选）「业务单据」上传内部账模板（表头含：业务单号 / 类型(应收/应付) / 客户供应商 / 金额 / 预计到账日期，列顺序随意）
3. 点「开始对账」，系统按三要素阶梯匹配：
   - ✅ L1 强匹配：金额相等 + 户名一致 → 自动打勾
   - ⚠️ L3 模糊关联：金额相等但户名略异 → 标"待确认"，人工核对
   - ✅ L2 容差匹配：金额差 ±5 元内 + 日期差 ≤3 天 → 自动通过并标记
   - 🟠 重复：单号已被核销又被匹配 → 橙字警示
4. 三个视图切换查看：
   - **对账明细**：逐笔匹配结果
   - **待办异议表**：🔴 长款（银行有账业务无单）/ 🔵 短款（业务有单银行无账）/ 🟠 重复，每条附核查建议
   - **回款看板**：按客户/供应商看"应付款 vs 已到账"，结清变绿、逾期显示天数
5. 「导出对账表」生成 Excel 存档；「AI 分析未匹配」解释对不上的原因（需配大模型）

> 没上传业务单据时，自动退化为与发票台账匹配，功能不失效。

### 5️⃣ 报表中心

**三块能力：**

**A. 财务报表上传分析（新）**：
- 一次上传多份：利润表 / 资产负债表 / 现金流量表（Excel/Word/PDF/OFD/图片均可），自动识别报表类型
- 支持标准财报格式和上市公司多期对比格式（东方财富等下载的年报，如贵州茅台）
- 一键分析输出：自动预警（亏损/高负债/现金流为负）→ 财务指标卡（毛利率/净利率/资产负债率/流动比率/三大现金流）→ 三表勾稽校验（资产=负债+权益、净利润↔未分配利润、期末现金↔货币资金）→ AI 4 段式文字报告

**B. 业务报表**：
- 周报 / 月报：基于发票台账自动汇总（收发对比、分类 Top5、环比提醒）
- 三张出表模板：损益对比（本月vs上月vs去年同期）/ 费用明细排行（按分类或供应商，Top3 高亮）/ 现金流简表（经营/投资/筹资自动归类）

**C. 分发**：导出 Excel / Word / 打印 PDF / 复制简报（可粘贴到微信、钉钉）。不发邮件。

---

## 三、系统设置（⚙ 右上角）

| 设置项 | 作用 | 是否必须 |
|---|---|---|
| 自家公司名称 | 识别购销方归属、生成备注 | 建议首次设置 |
| 腾讯云 OCR | 扫描件识别（¥0.6/次，每月 1000 次免费） | 有扫描件才需要 |
| 外部大模型 | AI 分析报告 / 字段纠错（推荐 DeepSeek，一次分析约 ¥0.01） | 可选，不配则用内置模板 |

大模型详细配置步骤见项目文件夹《**大模型配置说明.md**》。

---

## 四、数据保存机制（重要！）

### 自动保存（v4.2 起，关掉浏览器/刷新页面都不丢）

| 数据 | 存储位置 | 保存时机 |
|---|---|---|
| **发票台账**（14 列数据） | 浏览器 IndexedDB | 每次新增/编辑/删除后自动保存，重新打开自动恢复 |
| **银行流水 / 业务单据** | 浏览器 IndexedDB | 上传或清空时自动保存 |
| **对账结果**（明细/异议表/回款看板） | 浏览器 IndexedDB | 点「开始对账」后自动保存 |
| **财务报表 + 综合分析 + AI 分析报告** | 浏览器 IndexedDB | 上传/生成/移除时自动保存 |
| **周报/月报/出表模板** | 浏览器 IndexedDB | 生成时自动保存 |
| **系统设置**（公司名/Key 等） | 浏览器 localStorage | 点「保存设置」时 |

> 重新打开网站时会提示"已恢复上次工作现场：xxx"，所有结果自动还原，无需重做。
> 注意：上传新流水/新单据会使旧对账结果失效（自动同步清除），需重新点「开始对账」。

### 仍需手动备份的

| 数据 | 备份方法 |
|---|---|
| 发票**原文件** | IndexedDB 只存识别数据不存文件，用「打包下载」保存原件 |
| 各类结果的可分享版本 | 用「导出 Excel / Word / PDF」导出后发同事或归档 |

### ⚠️ 三个"会丢数据"的情况

1. **清除浏览器数据/缓存** → 台账和设置全部清空
2. **换浏览器或换电脑** → 数据不互通（线上版也一样，数据跟浏览器走）
3. **浏览器无痕/隐私模式** → 关窗即清空，不要在无痕模式记账

---

## 五、稳定使用建议（养成 3 个习惯）

> v4.2 起所有结果已自动保存，但浏览器数据仍有被清除的风险，导出备份永远是最后一道保险。

1. **每次用完导一次台账 Excel + 打包下载原件**
   - 这是最可靠的备份。Excel 是数据，zip 是原件，两者都留
   - 建议按月归档：`2026-08台账.xlsx` + `2026-08发票.zip`
2. **固定用一个浏览器**（推荐 Chrome 或 Edge）
   - 数据跟着浏览器走，今天用 Chrome 明天用 Edge，台账就对不上
3. **重要数据双保险**：导出的 Excel/zip 再存一份到网盘或 U 盘
   - 浏览器数据没有"账号找回"，清了就是清了

### 出问题时怎么办

| 症状 | 处理 |
|---|---|
| 打开页面台账空了 | 先确认是不是换了浏览器/无痕模式；若误清除，用备份 Excel 重新导入（台账管理支持上传 xlsx） |
| 线上版打不开/很慢 | 用方式 2 或方式 3 本地打开，数据若在同浏览器不受影响 |
| PDF 识别失败 | 文本型 PDF 断网也能识别；扫描件需联网 + 开启 OCR |
| AI 分析失败 | 见《大模型配置说明.md》第四节排查 |
| 导出 Excel 报"库未就绪" | 刷新页面重试（库加载慢时偶发） |

---

## 六、隐私与安全

- 发票文件**全部在浏览器本地解析**，不经过任何服务器
- 只有两种情况会外发数据：① 开启 OCR 时把扫描件图片发给腾讯云；② 启用大模型时把相关文本数据发给对应服务商
- API Key 只存本机 localStorage，代码开源可查（GitHub: WY133656/piaoxiaobang）

---

## 附：快速上手清单（新设备 5 分钟）

1. 打开 https://wy133656.github.io/piaoxiaobang/
2. ⚙ 设置 → 填「自家公司名称」→（可选）配 OCR / 大模型
3. 台账管理 → 上传本月发票 → 核对编辑 → 导出台账 Excel + 打包下载
4. 月底：报表中心 → 上传财务三表 → 一键分析 → 导出报告
5. 对账：自动对账 → 传银行流水 + 业务单据 → 开始对账 → 处理红蓝橙异议 → 导出
