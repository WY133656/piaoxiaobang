# 票小帮 · 大模型配置说明

> 适用版本：v4.1 及以上（commit b89e093）
> 更新日期：2026-08-18

配置大模型后，票小帮的三类 AI 功能自动启用；**未配置时自动降级为内置规则模板，其余功能完全不受影响**。

---

## 一、配置后能用的功能

| 功能 | 位置 | 说明 |
|---|---|---|
| 发票字段纠错 | 发票上传后自动触发 | 对 OCR/文本提取结果做字段级校对 |
| 4 段式财务分析报告 | 报表中心 → 上传财务报表 → 一键分析 → AI 文字分析 | 总体概览 / 费用异动预警 / 现金流健康度 / 业务提示，约 500 字 |
| 未匹配流水原因分析 | 自动对账 → AI 分析未匹配 | 解释银行流水与业务单据对不上的可能原因 |
| 周报/月报 AI 分析 | 报表中心 → 周报/月报 → AI 分析 | 基于台账快照的文字分析 |

---

## 二、推荐方案：DeepSeek（性价比最高）

### 第 1 步：申请 API Key（约 3 分钟）

1. 打开 **https://platform.deepseek.com**，手机号注册并登录
2. 左侧菜单「充值」→ 充 **10 元**即可长期使用
   - 计费：deepseek-chat 约 ¥1/百万输入字，一次财务分析约 ¥0.01
3. 左侧「API Keys」→「创建 API Key」
4. 复制生成的 `sk-` 开头密钥并妥善保存
   > ⚠️ Key **只在创建时显示一次**，关掉就看不到了；泄露可在列表里删除重建

### 第 2 步：在票小帮中填写

打开票小帮（本地版或线上版均可）→ 点右上角「**⚙ 设置**」→「**外部 AI 服务**」区块：

| 设置项 | 填写值 |
|---|---|
| 启用大模型 | **开启** |
| 接口类型 | OpenAI 兼容（DeepSeek/混元/通义…） |
| 接口地址（Base URL） | `https://api.deepseek.com` |
| 模型名称 | `deepseek-chat` |
| API Key | 你申请的 `sk-` 开头密钥 |

填完点「**保存设置**」。

### 第 3 步：验证

报表中心 → 上传财务报表（利润表/资产负债表/现金流量表）→ 一键分析 → 点「AI 文字分析」。
能生成完整报告即为配置成功；若提示失败，见下方常见问题。

---

## 三、其他服务商配置参数

均通过「OpenAI 兼容」或「Anthropic」接口类型接入，填法同理：

| 服务商 | 接口类型 | Base URL | 模型名称示例 |
|---|---|---|---|
| DeepSeek | OpenAI 兼容 | `https://api.deepseek.com` | `deepseek-chat` |
| 阿里通义（百炼） | OpenAI 兼容 | `https://dashscope.aliyuncs.com/compatible-mode/v1` | `qwen-plus` |
| 智谱 GLM | OpenAI 兼容 | `https://open.bigmodel.cn/api/paas/v4` | `glm-4-air` |
| 月之暗面 Kimi | OpenAI 兼容 | `https://api.moonshot.cn/v1` | `moonshot-v1-8k` |
| 火山方舟（豆包） | OpenAI 兼容 | `https://ark.cn-beijing.volces.com/api/v3` | 接入点 ID（ep-开头） |
| Anthropic Claude | Anthropic | `https://api.anthropic.com` | `claude-sonnet-4-5` |

> Anthropic 需要特殊网络环境，国内用户优先选 DeepSeek。

---

## 四、常见问题

**Q1：Key 保存在哪里？安全吗？**
保存在本机浏览器 localStorage，不上传任何服务器。换浏览器/换电脑需要重新填写；清除浏览器数据会丢失配置。

**Q2：调用 AI 会发送什么数据？**
仅发送与当前功能相关的文本数据（如报表指标汇总、发票字段）到对应服务商。发票原件、图片等文件不会上传。

**Q3：没配置 Key 会怎样？**
AI 功能自动降级为内置规则模板（基于固定规则的文字报告），功能入口不消失，其他功能零影响。

**Q4：提示"AI 分析失败"怎么排查？**
1. 检查 Key 是否复制完整（`sk-` 开头无多余空格）
2. DeepSeek 账户是否还有余额
3. 接口地址是否填了多余路径（正确值就是 `https://api.deepseek.com`，不要加 `/v1`）
4. 线上版（GitHub Pages）需网络能访问对应服务商 API

**Q5：费用大概多少？**
DeepSeek deepseek-chat：一次完整财务分析约 ¥0.01，日常月度使用每月通常不到 ¥1。

**Q6：能同时配 OCR 和大模型吗？**
可以，两者独立。OCR（腾讯云）负责扫描件识别，大模型负责纠错和分析，互不冲突。
