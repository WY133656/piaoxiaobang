# 腾讯云域名申请指引（票小帮专属域名）

票小帮目前通过 GitHub Pages 免费托管在 `https://<你的用户名>.github.io/票小帮/`。申请一个自己的域名后，可以变成 `https://www.你的域名.com` 这样的正式地址，方便给客户、老板、合作伙伴分享，也更专业。

> 费用预估：普通 `.com` 域名首年约 **55-80 元**，续费约 80-100 元/年；`.cn` 域名首年约 30 元左右（经常有新人优惠，最低几块钱）。备案免费。

---

## 一、选一个合适的域名

| 方案 | 示例 | 适用场景 |
|---|---|---|
| 拼音直拼 | `piaoxiaobang.com` | 直接对应产品名，好记但可能已被注册 |
| 拼音+关键词 | `piaozhuli.com`、`piaodabang.com` | 体现「发票+助手」定位 |
| 英文组合 | `invoice-helper.com`、`fapiao-ai.com` | 国际化一点，或用 fapiao 拼音 |
| 行业后缀 | `piaoxiaobang.cn` / `.com.cn` | 国内用户用 .cn 更便宜 |

**取名建议：**
- 优先 `拼音/单词 + 后缀` 组合，避免数字和连字符（容易记错）
- 在腾讯云搜索框里输入候选名，会直接显示「可注册 / 已被注册」和价格
- 如果 `.com` 被抢注，可以试试 `.cn`、`.com.cn`、`.net`，或用 `ai`、`pro`、`vip` 等后缀（常有首年低价）

## 二、注册流程（全程约 5 分钟）

1. 打开腾讯云域名注册：<https://dnspod.cloud.tencent.com/>
2. 顶部搜索框输入你想要的域名（如 `piaoxiaobang`），回车
3. 在结果列表勾选「可注册」的域名和后缀，看价格：
   - 有「新人价」「首年特惠」标识的优先选
4. 点击**加入购物车** → **立即购买**
5. 选择购买年限（首年便宜，建议先买 1 年试水）
6. 填写域名信息模板：
   - 没有模板就点「新建模板」→ 填写姓名、联系方式（**必须是真实信息**，用于实名认证）
   - 个人注册选「个人用户」，企业注册选「企业用户」
7. 确认订单 → 支付（支持微信/支付宝）
8. 支付成功后进入**域名管理**：<https://console.cloud.tencent.com/domain>

> ⚠️ 购买完成后**必须做实名认证**，否则域名会被暂停解析，无法访问。

## 三、域名实名认证

1. 在域名管理列表，找到刚买的域名，状态显示「未实名」或「待实名」
2. 点击**实名认证**：
   - 个人：上传身份证正反面照片，约 1 分钟自动审核
   - 企业：上传营业执照，审核约 1 个工作日
3. 实名通过后，域名状态变为「正常」

## 四、解析到票小帮（GitHub Pages）

买好域名后，把域名指向 GitHub Pages：

1. 在 GitHub 打开票小帮仓库 → **Settings** → **Pages**
2. 在「Custom domain」输入框填你的域名（如 `piaoxiaobang.com`），点 **Save**
3. 回到腾讯云域名管理 → 点域名后面的**解析**
4. 添加两条解析记录：

| 记录类型 | 主机记录 | 记录值 | 说明 |
|---|---|---|---|
| A | @ | `185.199.108.153` | 主域名指向 GitHub Pages |
| A | @ | `185.199.109.153` | 备用 IP，两个都加上 |
| A | @ | `185.199.110.153` | 备用 IP |
| A | @ | `185.199.111.153` | 备用 IP |
| CNAME | www | `<你的用户名>.github.io` | www 子域名也指向 |

> GitHub Pages 的 IP 请以 GitHub 官方文档为准：<https://docs.github.com/cn/pages/configuring-a-custom-domain-for-your-github-pages-site/managing-a-custom-domain-for-your-github-pages-site>

5. 等待解析生效（通常几分钟到几小时），访问 `http://你的域名` 验证
6. 在 GitHub Pages 设置里勾选 **Enforce HTTPS**（强制 HTTPS，GitHub 会自动签发证书，一般 1 小时内生效）

## 五、需要备案吗？

| 托管位置 | 是否要备案 |
|---|---|
| GitHub Pages（海外服务器） | **不需要**备案，买完域名解析即可用 |
| 腾讯云服务器/云托管（境内） | 需要备案，流程约 7-20 天 |

票小帮目前托管在 GitHub Pages，**域名不用备案**。如果以后把票小帮迁到国内服务器（访问更快更稳），再走备案流程：

- 腾讯云备案入口：<https://beian.tencent.com/>

## 六、后续维护

- **续费提醒**：腾讯云会在到期前 30 天短信/邮件提醒，记得及时续费，过期域名会被收回
- **域名转移**：腾讯云买的域名可以随时转出到其他平台（转出需实名且无欠费）
- **邮箱**：可以开通腾讯企业邮箱（免费版）用 `你的名字@你的域名` 收信：<https://cloud.tencent.com/product/exmail>

---

## 常见问题

**Q：买完域名多久能访问？**
A：实名认证通过 + 解析生效后即可访问。A 记录解析通常 5-30 分钟生效，HTTPS 证书签发最多 24 小时。

**Q：域名被注册了怎么办？**
A：换后缀（.cn/.net/.ai）或加词（如 `piaoxiaobang-ai.com`）。也可以先注册 `.cn`（便宜），以后有钱再收 `.com`。

**Q：一个人能注册几个域名？**
A：个人实名后可注册多个，没有数量限制。但每个域名都要单独实名。

**Q：GitHub Pages 在国内访问稳定吗？**
A：大部分地区可用，个别网络环境下可能不稳定。如果公司/客户访问频繁，建议后续把票小帮部署到国内平台（如腾讯云 COS 静态网站托管 + 备案域名），也可以找我来帮你配置。
